loki:
  enabled: true
  persistence:
    enabled: false  # Minikube에서는 볼륨 없이 실행

promtail:
  enabled: true
  config:
    clients:
      - url: http://loki.monitoring.svc:3100/loki/api/v1/push

grafana:
  enabled: false