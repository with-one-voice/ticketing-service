apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: onevoice
spec:
  selector:
    app: gateway
  ports:
    - name: metrics
      port: 8080
      targetPort: gateway-port
  type: ClusterIP
