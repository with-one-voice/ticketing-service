apiVersion: v1
kind: Service
metadata:
  name: venue-service
spec:
  selector:
    app: venue
  ports:
    - port: 8004           # 외부에서 접근할 포트
      targetPort: venue-port   # 컨테이너 내부 포트 (Deployment의 containerPort와 일치)
  type: ClusterIP        # 내부 서비스용 (기본값)
