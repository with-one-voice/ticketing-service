name: Deploy Auth Service

on:
  push:
    branches: [ develop,"feature/**" ]
    paths:
      - 'auth/**'
      - '.github/workflows/deploy-auth.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: .

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: ✅ Grant execute permission to gradlew
        run: chmod +x ./gradlew

      - name: 🧪 Build JAR
        run: ./gradlew :auth:build -x test

      - name: 🔖 Set Git SHA tag
        id: vars
        run: echo "SHORT_SHA=$(echo $GITHUB_SHA | cut -c1-7)" >> $GITHUB_ENV

      - name: 🐳 Docker Build & Push
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/auth-service:${SHORT_SHA} .
          docker tag ${{ secrets.DOCKER_USERNAME }}/auth-service:${SHORT_SHA} ${{ secrets.DOCKER_USERNAME }}/auth-service:latest
          
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push ${{ secrets.DOCKER_USERNAME }}/auth-service:${SHORT_SHA}
          docker push ${{ secrets.DOCKER_USERNAME }}/auth-service:latest

      - name: 🔐 Create Kubernetes Secret from GitHub Secrets
        run: |
          kubectl create secret generic onevoice-secret \
            --from-literal=DB_USERNAME=${{ secrets.DB_USERNAME }} \
            --from-literal=DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
            --from-literal=JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }} \
            --dry-run=client -o yaml | kubectl apply -f -
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG }}

      - name: 🚀 Deploy to Kubernetes
        run: |
          sed -i "s|image: .*|image: ${{ secrets.DOCKER_USERNAME }}/auth-service:${SHORT_SHA}|" k8s/deployment.yaml
          kubectl apply -f k8s/
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG }}
